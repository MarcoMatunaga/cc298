subroutine thomas_block_tridiagonal_v1(lower,main,upper,x_v,d_v,diag)
    use vars
    implicit none
    double precision lower(dim,dim), main(dim,dim), upper(dim,dim)
    double precision x_v(dim), d_v(dim)
    integer diag
    !
    !
    double precision gama_matrix(dim,dim)
!
!
!

end subroutine thomas_block_tridiagonal_v1